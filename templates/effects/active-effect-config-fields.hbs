{{!-- Custom fields for Vagabond Active Effect Config --}}

<div class="vagabond-effect-config-wrapper">
  <fieldset class="vagabond-effect-fields">
    {{!-- <legend>{{localize "VAGABOND.Effect.ApplicationMode.Label"}}</legend> --}}

    <div class="form-group">
      <label for="application-mode">{{localize "VAGABOND.Effect.ApplicationMode.Label"}}</label>
      <div class="form-fields">
        <select name="flags.vagabond.applicationMode" id="application-mode">
          {{#each vagabond.applicationModes as |mode key|}}
            <option value="{{key}}" {{#if (eq ../vagabond.applicationMode key)}}selected{{/if}}>
              {{localize mode.label}}
            </option>
          {{/each}}
        </select>
      </div>
      <p class="hint application-mode-hint"></p>
    </div>

    {{#if vagabond.parentItemName}}
    <div class="form-group info-box">
      <i class="fas fa-info-circle"></i>
      <p class="notes">
        <strong>{{localize "VAGABOND.UI.Labels.ParentItem"}}</strong> {{vagabond.parentItemName}} ({{vagabond.parentItemType}})<br>
        <strong>{{localize "VAGABOND.UI.Labels.SuggestedMode"}}</strong> {{localize vagabond.smartDefaultMode.label}}
      </p>
    </div>
    {{/if}}

    <div class="form-group help-box">
      <details>
        <summary><i class="fas fa-question-circle"></i> {{localize "VAGABOND.UI.Labels.ApplicationModeGuide"}}</summary>
        <div class="mode-help">
          {{#each vagabond.applicationModes as |mode key|}}
          <div class="mode-example">
            <h4><i class="{{mode.icon}}"></i> {{localize mode.label}}</h4>
            <p>{{localize mode.hint}}</p>
            {{#if (eq key "permanent")}}
            <p class="example"><em>{{localize "VAGABOND.UI.Examples.PermanentEffect"}}</em></p>
            {{else if (eq key "when-equipped")}}
            <p class="example"><em>{{localize "VAGABOND.UI.Examples.EquippedEffect"}}</em></p>
            {{else if (eq key "on-use")}}
            <p class="example"><em>{{localize "VAGABOND.UI.Examples.OnUseEffect"}}</em></p>
            {{/if}}
          </div>
          {{/each}}
        </div>
      </details>
    </div>

    <div class="form-group help-box">
      <details>
        <summary><i class="fas fa-code"></i> Formula Support for Effect Values</summary>
        <div class="mode-help">
          <div class="mode-example">
            <h4><i class="fas fa-calculator"></i> Using Formulas</h4>
            <p>Effect values support both simple numbers and dynamic formulas. Start typing <code>@</code> to see autocomplete suggestions.</p>
            <p class="example"><em>Examples:</em></p>
            <ul style="margin: 0.5rem 0; padding-left: 1.5rem;">
              <li><code>5</code> - Simple number</li>
              <li><code>@attributes.level.value</code> - Character level</li>
              <li><code>floor(@attributes.level.value / 2)</code> - Half level (rounded down)</li>
              <li><code>@stats.might.total</code> - Might stat total</li>
              <li><code>@might.value + @dexterity.value</code> - Sum of two stats</li>
            </ul>
          </div>
          <div class="mode-example">
            <h4><i class="fas fa-star"></i> Common Scaling Perks</h4>
            <p class="example"><strong>Secret of Mana</strong> (Casting Max = Level):</p>
            <ul style="margin: 0.25rem 0; padding-left: 1.5rem; font-size: 0.9em;">
              <li>Attribute Key: <code>system.mana.castingMaxBonus</code></li>
              <li>Effect Value: <code>@attributes.level.value</code></li>
            </ul>
            <p class="example" style="margin-top: 0.75rem;"><strong>Tough</strong> (+1 HP per Level):</p>
            <ul style="margin: 0.25rem 0; padding-left: 1.5rem; font-size: 0.9em;">
              <li>Attribute Key: <code>system.bonuses.hpPerLevel</code></li>
              <li>Effect Value: <code>1</code></li>
            </ul>
            <p class="example" style="margin-top: 0.75rem;"><strong>Scaling Armor</strong> (Armor = Dexterity):</p>
            <ul style="margin: 0.25rem 0; padding-left: 1.5rem; font-size: 0.9em;">
              <li>Attribute Key: <code>system.armorBonus</code></li>
              <li>Effect Value: <code>@stats.dexterity.total</code></li>
            </ul>
          </div>
          <div class="mode-example">
            <h4><i class="fas fa-function"></i> Available Variables</h4>
            <p>Type <code>@</code> in the Effect Value field to see all available variables including:</p>
            <ul style="margin: 0.5rem 0; padding-left: 1.5rem;">
              <li><strong>Attributes:</strong> level, xp, isSpellcaster, etc.</li>
              <li><strong>Stats:</strong> might, dexterity, awareness, reason, presence, luck</li>
              <li><strong>Skills:</strong> arcana, craft, medicine, etc.</li>
              <li><strong>Math:</strong> floor(), ceil(), round(), abs(), min(), max()</li>
            </ul>
          </div>
        </div>
      </details>
    </div>

  </fieldset>
</div>
